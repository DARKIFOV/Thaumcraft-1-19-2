# GitHub release v11.62.44

Build with Java 17 and Forge 43.5.2:

```text
./gradlew build --stacktrace --no-daemon
```

The playable file is `build/libs/thaumcraft_legacy_rebuild_1.19.2-11.62.44.jar`.
Do not place the source ZIP in the Minecraft `mods` directory.
