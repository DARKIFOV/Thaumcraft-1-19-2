org.gradle.jvmargs=-Xmx3G -Dfile.encoding=UTF-8
org.gradle.daemon=false
# ForgeGradle 5.1 artifact transforms race compileJava after a clean build on
# Windows when parallel execution is enabled, producing an empty MC classpath.
org.gradle.parallel=false
org.gradle.caching=true
