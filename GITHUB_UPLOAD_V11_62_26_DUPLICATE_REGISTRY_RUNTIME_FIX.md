# GitHub upload — v11.62.26

Upload the contents of this archive to the repository root and run the `Forge 1.19.2 Build` workflow.

The resulting playable artifact is named:

`thaumcraft-legacy-rebuild-1.19.2-v11.62.26-github-jar`

This version fixes the Forge startup crash:

`IllegalArgumentException: Duplicate registration tc4_crystalessence`
